name: Poll internship listings (:37)

"on":
  schedule:
    - cron: "37 * * * *"
  workflow_dispatch: {}

concurrency:
  group: careerview-poll
  cancel-in-progress: false

jobs:
  poll:
    permissions:
      contents: write
    uses: ./.github/workflows/_poll-run.yml
    secrets: inherit
