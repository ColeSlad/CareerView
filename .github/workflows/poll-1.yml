name: Poll internship listings (:07)

"on":
  schedule:
    - cron: "7 * * * *"
  workflow_dispatch: {}

concurrency:
  group: careerview-poll
  cancel-in-progress: false

jobs:
  poll:
    permissions:
      contents: write
    uses: ./.github/workflows/_poll-run.yml
    secrets: inherit
