name: Poll internship listings (:52)

"on":
  schedule:
    - cron: "52 * * * *"
  workflow_dispatch: {}

concurrency:
  group: careerview-poll
  cancel-in-progress: false

jobs:
  poll:
    uses: ./.github/workflows/_poll-run.yml
    secrets: inherit
